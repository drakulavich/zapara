assets/*.mp4 filter=lfs diff=lfs merge=lfs -text
assets/*.webp filter=lfs diff=lfs merge=lfs -text
# The card sheet ships as a plain file so a clone without git-lfs renders cards, and the
# README's example card is plain so clones do not spend the LFS bandwidth quota on it.
assets/characters.webp -filter -diff -merge
assets/card.webp -filter -diff -merge
docs/superpowers/specs/assets/*.webp filter=lfs diff=lfs merge=lfs -text
assets/*.png filter=lfs diff=lfs merge=lfs -text
